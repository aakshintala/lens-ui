# Pinned for reproducibility — matches the ground-truth/pinning discipline used
# for the omnigent contract. Bump deliberately, not incidentally.
[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy", "rust-analyzer"]
profile = "minimal"
